# Caminhada no cipó — versão aprovada antes do ajuste dos apoios

Snapshot solicitado pelo usuário em 16/09/2026. Inclui o percurso de ida e volta e a pose anterior.

Para restaurar, na raiz do projeto:

```sh
tar -xzf backups/vine-walk-before-outer-grip/source.tar.gz
```

A restauração sobrescreve apenas os oito arquivos listados em `manifest.json`.
